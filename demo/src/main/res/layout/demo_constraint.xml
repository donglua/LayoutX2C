<?xml version="1.0" encoding="utf-8"?>
<!-- demo_constraint: ConstraintLayout safe subset (anchors + bias) -->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <TextView
        android:id="@+id/constraint_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:text="@string/demo_constraint_title"
        android:textSize="16sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/constraint_subtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/constraint_title"
        app:layout_constraintStart_toStartOf="@id/constraint_title"
        android:text="@string/demo_constraint_subtitle"
        android:textSize="14sp" />

    <TextView
        android:id="@+id/constraint_end"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/constraint_title"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="1.0"
        android:text="@string/demo_constraint_end"
        android:textSize="12sp"
        android:textColor="@color/demo_text_primary" />

</androidx.constraintlayout.widget.ConstraintLayout>
